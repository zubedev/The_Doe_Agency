# COPY THE .dev-env TO PROJECT ROOT AND RENAME TO .env

# run the following function in django shell to generate a secret key
# django.core.management.utils.get_random_secret_key()
SECRET_KEY=
ALLOWED_HOSTS=['*']
DEBUG=True

DB_ENGINE=django.db.backends.postgresql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=tda_db
DB_USER=tda_user
DB_PASS=tda_pass

TIME_ZONE=Australia/Perth
